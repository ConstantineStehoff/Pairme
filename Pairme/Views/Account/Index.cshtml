@*@model Pairme.Models.RegisterViewModelStep1*@
@using Microsoft.Web.Mvc
@model Pairme.Models.WizardViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var currentStep = Model.Steps[Model.CurrentStepIndex];
}

@*<div class="container">
    @using (Html.BeginForm("Index", "Account", FormMethod.Post, new { @class = "form-horizontal ds-initForm", role = "form" }))
    {
        <div class="form-group">
            @Html.LabelFor(model => model.Gender, "I am a")
            @Html.DropDownListFor(model => model.Gender, new SelectList(
                      new List<Object>{
                           new { value = 0 , text = "Woman"  },
                           new { value = 1 , text = "Man" }
                        },
                      "value",
                      "text",
                       2))
        </div>
        
        <div class="form-group">
            @Html.LabelFor(model => model.MatchGender, "Looking for a")
            @Html.DropDownListFor(model => model.MatchGender, new SelectList(
                      new List<Object>{
                           new { value = 0 , text = "Man"  },
                           new { value = 1 , text = "Woman" }
                        },
                      "value",
                      "text",
                       2))
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Country, "Country")
            @Html.DropDownListFor(model => model.Country, new SelectList(
                      new List<Object>{
                           new { value = 0 , text = "United States"  },
                           new { value = 1 , text = "Canada" }
                        },
                      "value",
                      "text",
                       2))
        </div>

        <div class="form-group">
            @Html.TextBoxFor(m => m.ZipCode, new { @class = "form-control", placeholder = "Zip Code" })
        </div>
        
        <button type="submit" class="btn btn-primary">Browse</button>
        <button type="submit" class="btn btn-primary">Register</button>
    }
</div>*@

<div class="container">
    <h3>Step @(Model.CurrentStepIndex + 1) out of @Model.Steps.Count</h3>
    @using (Html.BeginForm())
    {
        @Html.Serialize("wizard", Model)

        @Html.Hidden("StepType", Model.Steps[Model.CurrentStepIndex].GetType())
        @Html.EditorFor(x => currentStep, null, "")

        if (Model.CurrentStepIndex > 0)
        {
            <input type="submit" value="Previous" name="prev" />
        }

        if (Model.CurrentStepIndex < Model.Steps.Count - 1)
        {
            <input type="submit" value="Next" name="next" />
        }
        else
        {
            <input type="submit" value="Finish" name="finish" />
        }
    }    
</div>
